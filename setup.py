#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='snn_utils',
      version='0.1',
      description='Utilitiy collection for spiking neural networks.',
      author='Michael Hoff',
      author_email='mail@michael-hoff.net',
      #package_dir={},
      packages=find_packages(),
      )
